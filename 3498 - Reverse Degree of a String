class Solution {
    public int reverseDegree(String s) {

        //TC = O(n)
        //SC = O(1)

        int Idx = 1;
        int result = 0;
        for(char c : s.toCharArray())
            result += (26-(c-'a')) * Idx++;
        
        return result;
    }
}
